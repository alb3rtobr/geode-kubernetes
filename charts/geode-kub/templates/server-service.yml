apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{.Values.geode_server.name}}-service
    role: {{.Values.geode_server.name}}
  name: {{.Values.geode_server.name}}-service
spec:
  ports:
  - port: {{.Values.geode_server.port}}
    name: peer
  clusterIP: None
  selector:
    geode-server: "true"
